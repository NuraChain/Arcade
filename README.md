# Nura Games

A cinematic 3D landing experience for a social gaming platform — a night market of floating game
tables that the reader travels through by scrolling.

Built on AzerothJS and Three.js. `npm run dev` to start, `npm run check` to gate.

MIT.
