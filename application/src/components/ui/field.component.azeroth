import type { Child } from 'azerothjs';

import { useLocale } from '../../stores/locale.store.ts';

export default component Field(props: {
    children?: Child;
    id: string;
    label: string;
    hint?: string;
    error?: string | null;
    optional?: boolean;
    class?: string;
})
{
    const locale = useLocale();

    <div class={ [ 'flex flex-col gap-1.5', props.class ?? '' ].filter(Boolean).join(' ') }>
        <label for={ props.id } class="flex items-baseline justify-between gap-3 text-ui-sm font-semibold text-text">
            <span>{ props.label }</span>
            <Show when={ props.optional === true }>
                { () => <span class="text-ui-xs font-normal text-faint">{ locale.t('common.optional') }</span> }
            </Show>
        </label>
        { props.children }
        <Show
            when={ props.error !== undefined && props.error !== null }
            fallback={
                <Show when={ props.hint !== undefined }>
                    { () => <p id={ `${ props.id }-hint` } class="text-ui-xs text-muted">{ props.hint }</p> }
                </Show>
            }
        >
            { () => <p id={ `${ props.id }-error` } class="text-ui-xs font-medium text-madder" role="alert">{ props.error }</p> }
        </Show>
    </div>
}
