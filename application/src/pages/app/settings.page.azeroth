import { useHead } from 'azerothjs';

import Page from '../../components/app/page.component.azeroth';
import EmptyState from '../../components/ui/empty-state.component.azeroth';
import { useLocale } from '../../stores/locale.store.ts';

export default component SettingsPage()
{
    const locale = useLocale();

    useHead({ title: () => locale.t('app.nav.settings') });

    <Page>
        <h1 class="text-[clamp(1.75rem,5vw,2.5rem)]">{ locale.t('app.nav.settings') }</h1>
        <EmptyState icon="sparkles" title={ locale.t('state.emptyTitle') } />
    </Page>
}
